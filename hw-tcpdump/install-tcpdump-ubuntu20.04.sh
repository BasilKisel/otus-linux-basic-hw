#!/bin/bash

# This script silently installs tcpdump.
# Appropriate system permissions are needed to perform the task.
# In general case scenario use `sudo install-tcpdump-ubuntu20.04.sh`

apt-get -y install tcpdump
