# This configuration for mysql-source VM for Otus Linux Bacis Course.
# mysql-source is always at 192.168.1.13
network:
  renderer: networkd # networkd is a default for now, but who knows all posibilities
  ethernets:
    eth0:
      dhcp4: true
      addresses:
        - 192.168.1.13/24
  version: 2
